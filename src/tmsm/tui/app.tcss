Screen {
    layout: vertical;
}

#table-wrap {
    height: 1fr;
    border: round $accent;
}

#details {
    height: auto;
    max-height: 50%;
    border: round $accent;
    padding: 1 2;
}

#details-body {
    height: auto;
}

DataTable {
    height: 1fr;
}

.dim {
    color: $text-muted;
}
