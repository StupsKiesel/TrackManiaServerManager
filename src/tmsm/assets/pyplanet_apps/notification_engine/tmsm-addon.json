{
  "description": "notification_engine: transient on-screen toast notifications. Any app can fire a message via the notification_engine:notify signal; messages slide in from the right, stack top-right, and slide out left after a timeout (or wait for a button click). The anchor is configurable through widget_engine.",
  "author": "tmsm"
}
